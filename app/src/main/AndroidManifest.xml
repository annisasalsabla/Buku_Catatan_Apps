<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.annisa.bukucatatan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Buku Catatan"
        android:theme="@style/Theme.BukuCatatan">

        <!-- MainActivity: The entry point of the app -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Buku Catatan"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AddNoteActivity: Activity for adding a new note -->
        <activity
            android:name=".AddNoteActivity"
            android:label="Add Note"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- EditNoteActivity: Activity for editing an existing note -->
        <activity
            android:name=".EditNoteActivity"
            android:label="Edit Note"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- DetailNoteActivity: Activity to view the details of a note -->
        <activity
            android:name=".DetailNoteActivity"
            android:label="Detail Note"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

    </application>
</manifest>